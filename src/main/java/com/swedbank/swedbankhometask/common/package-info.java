/**
 * Shared building blocks used by every module. Open so that all modules may depend on it.
 *
 * @author vinodjohn
 * @since 22.07.2026
 */
@ApplicationModule(type = ApplicationModule.Type.OPEN)
package com.swedbank.swedbankhometask.common;

import org.springframework.modulith.ApplicationModule;
